"""
Core package initializer.

Ensures the 'core' directory is recognized as a proper Python package by tools
and static analyzers (e.g., Pylance). Keep this file non-empty to avoid indexing
issues that can break relative imports like [`from .config_management import ...`](core/config_management.py:219).
"""
